extend ../layout

block content
    h1.mt-2 Liste des Utilisateurs
        - const messages = getMessages()
        if messages.errors
            p= messages.errors
    ul
        each user in users
            li 
                p= user.user_name